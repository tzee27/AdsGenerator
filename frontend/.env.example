# Base URL of the FastAPI backend (no trailing slash, must include /api/v1).
# In dev with the default uvicorn run script, this is http://localhost:8000/api/v1.
VITE_API_URL=http://localhost:8000/api/v1
